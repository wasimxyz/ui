node_modules
.DS_Store
*.log
.env
.env.*
!.env.template

# Machine-local Claude Code settings (the shared CLAUDE.md is committed).
.claude/settings.local.json

# NOTE: registry/ holds both the component source and the built registry JSON,
# both intentionally committed.
