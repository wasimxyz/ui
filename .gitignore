node_modules
.DS_Store
*.log
.env
.env.*
!.env.template

# Machine-local Claude Code settings (the shared CLAUDE.md is committed).
.claude/settings.local.json

# NOTE: registry/ holds the component source and public/registry/ holds the
# built registry JSON. public/registry/ is intentionally committed (not ignored)
# so the @wasimxyz registry resolves over raw.githubusercontent.com.
