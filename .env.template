# Copy to .env.local to show live GitHub data in the demo. Without these the
# fetch-github-contributions loader returns an empty series (it degrades
# gracefully), so the demo still runs with no secrets.

# Classic personal access token with `repo` + `read:user` scope.
GITHUB_TOKEN=

# The GitHub account to chart.
GITHUB_USERNAME=
